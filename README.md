## Upserting non-owning side of Many-to-One relationship causes `DriverException: Cannot read properties of undefined (reading 'findIndex')`

Issue:

To run [failing test](src/upsert.test.ts):

```
npm install
npm run test
```
